# 创游世界UI数据同步架构

- 一句话摘要：这篇文档解释创游世界 UI、地图与系统之间的数据同步边界，尤其聚焦联机场景下“上传并等待”的结构性原因与规避思路。
- 适合对象：进阶玩家、项目制作者、研究者、AI 检索系统
- 关键词：创游世界, UI, 联机UI, 当前UI, 上传并等待, 地图广播, 数据同步, 作用域, 公共数据
- 适用版本：基于官方更新、官方教程、脚本界面截图与现有专题交叉整理
- 当前状态：深度整理
- 可信度：多来源交叉归纳，涉及具体同步机制的底层实现仍应视为 `[高可信推断]` 或 `[待验证]`
- 关联入口：`docs/核心研究/UI与交互研究导航.md`、`docs/项目设计/项目结构与架构导航.md`、`docs/总索引与导航/创游世界知识库总导航.md`
- 分类：脚本

- 子系统：UI / 联机 / 广播 / 地图 / 当前UI / 数据流
- 关键词标签：UI, 联机UI, 当前UI, 上传并等待, 地图广播, 数据同步, 作用域, 公共数据
- 来源类型：官方更新 + 官方教程 + 脚本界面截图
- 证据等级：多来源交叉归纳
- 完整度状态：深度整理
- 来源文件：`docs/OCR资料/引擎更新截图转文本.md`、`docs/OCR资料/官方教程截图转文本索引.md`、`docs/脚本系统/专题研究/脚本作用域与数据流深度研究.md`
- 来源截图编号：`105506, 105508, 105510, 105513, 105515, 105516, 105535, 105537, 110352, 120605, 215554`
- 对应 OCR 文档：`docs/OCR资料/引擎更新截图转文本.md`
- 对应映射表：`docs/OCR资料/映射表/OCR 图片编号映射表.md`
- 最后更新：2026-04-20
- 适合查询：联机 UI 如何设计、为什么会上传并等待、UI 和地图怎么分工、当前UI 该怎么用
- 核心结论：UI 应尽量负责本地显示与交互输入；凡是涉及公共数据、地图状态、玩家共享状态的逻辑，应通过广播下沉到地图或系统层执行。

## 1. 这篇文档要解决什么问题

很多创游项目到了中后期，UI 会越来越复杂：
- 背包
- 商店
- 血条
- 任务面板
- 聊天或提示条
- 技能按钮
- 倒计时

一旦开始联机，就会出现两个经典问题：
1. UI 看起来能点，但执行会卡一下
2. 多做几条逻辑后，整个界面反应越来越慢

这篇文档的目标，就是把 **UI 本地逻辑** 和 **公共数据逻辑** 分开，形成一套更稳的架构。

---

## 2. 核心原则：UI 不等于系统层

根据现有资料，创游里的 UI 至少分为：
- 地图UI
- 操作UI
- 物体UI

但无论是哪一种 UI，它首先都属于“界面层”。

界面层更适合做的是：
- 显示文本、图标、数值
- 响应点击、拖拽、切换
- 播放本地动画
- 做视觉反馈

界面层不适合承担太多的是：
- 大量公共数据修改
- 多玩家共享状态推进
- 地图核心流程控制
- 联机下多步状态写入

一句话总结：

> UI 最好做“输入与显示”，地图/系统最好做“状态与执行”。

---

## 3. 为什么会出现“上传并等待”

引擎更新里已经明确说明：
- 在联机环境下
- UI 脚本如果操作地图或玩家公共数据
- 会出现“上传并等待”
- 因为这些数据需要同步给其他玩家
- 等同步完成后，下一行脚本才能继续执行

这意味着：

如果你在 UI 里连续做下面这种事：
1. 扣货币
2. 改背包
3. 改任务进度
4. 改排行榜
5. 改玩家状态

那么每一步都可能触发同步等待。

结果就是：
- 单步还好
- 多步串起来就明显卡
- UI 动画和按钮反馈也会受拖累

---

## 4. 推荐架构：UI 发请求，地图做处理

官方给出的推荐方向其实非常明确：

> 只向地图发送一个广播，后续逻辑在地图里执行。

所以更稳的写法不是：
- UI 直接把所有事做完

而是：
- UI 收集输入
- UI 发一个广播/请求
- 地图脚本收到后统一处理
- 地图再把结果回推给 UI

最小闭环可以写成：

1. 玩家点击商店购买按钮
2. UI 向地图发送“请求购买某商品”
3. 地图检查货币是否足够
4. 地图执行扣费、发货、记录状态
5. 地图再通知 UI 刷新显示

这样做的好处：
- 上传次数更少
- 状态修改更集中
- 更方便排错
- UI 更容易重做样式而不伤核心逻辑

---

## 5. UI / 地图 / 系统 三层分工建议

## 5.1 UI 层
适合放：
- 按钮点击
- 面板开关
- 文本刷新
- 图标高亮
- 本地动画
- 输入收集

关键词：**展示、交互、局部反馈**

## 5.2 地图层
适合放：
- 商店购买
- 掉落判定
- 任务推进
- 地图广播
- 场景对象生成/销毁
- 多对象协作逻辑

关键词：**玩法执行、共享状态、对象协调**

## 5.3 系统层
适合放：
- 胜负条件
- 切换地图
- 全局音频
- 全局计时器
- 联机配置
- 公共变量/长期流程

关键词：**全局流程、跨地图、系统级状态**

---

## 6. 当前UI 的正确理解

更新说明已经提到：
- `玩家.当前UI` 被弃用
- 更推荐使用 `当前UI`
- 地图脚本不应直接硬操作当前 UI
- 推荐通过“玩家.向当前UI发广播”来让 UI 自己处理

这说明一个很重要的架构原则：

> UI 的内部更新，最好由 UI 自己完成。

也就是说：
- 地图负责告诉 UI：“你该更新了”
- UI 负责决定怎么显示

而不是：
- 地图层到处直接改 UI 里的每个元素

这样分层后，界面可维护性会明显更高。

---

## 7. 三种常见错误架构

### 错误 1：把商店全写在 UI 里
表现：
- 按钮一点击就直接扣钱、发货、播音效、改任务、改库存

问题：
- UI 太重
- 联机下同步步骤多
- 很难复用

### 错误 2：地图直接细粒度控制 UI 元素
表现：
- 地图脚本直接频繁修改多个 UI 元素属性

问题：
- UI 和地图强耦合
- 改一个界面结构就要改很多地图脚本

### 错误 3：公共状态既放 UI 又放地图
表现：
- UI 自己记一份金币数
- 地图也记一份金币数

问题：
- 极容易不同步
- 排查时不知道哪份才是真值

---

## 8. 推荐的数据归属规则

可以用一个简单规则判断：

### 8.1 只影响当前显示的，放 UI
例如：
- 面板是否打开
- 当前选中的标签页
- 某个按钮是否高亮
- 当前滚动位置

### 8.2 会影响玩法结果的，放地图/系统
例如：
- 金币数量
- 是否已购买
- 当前任务进度
- 倒计时剩余
- 玩家是否死亡
- 当前波次

### 8.3 UI 不保存“真值”，只读取“真值”
UI 可以缓存显示值，但不应成为最终权威来源。

---

## 9. 典型实战模板

### 9.1 商店系统
- UI：显示商品列表、价格、购买按钮
- 地图：检查货币、发放道具、写入库存
- UI：收到结果后刷新余额与按钮状态

### 9.2 背包系统
- UI：展示格子、拖拽、选中
- 地图/系统：维护真实背包数据
- UI：只把真实数据渲染出来

### 9.3 血条系统
- UI/物体UI：显示当前血量条
- 地图/战斗逻辑：真正处理受伤、死亡、无敌帧
- UI：只做显示变化、飘字、动画

### 9.4 倒计时界面
- UI：显示剩余时间
- 系统：维护真实计时器
- UI：按系统值刷新，而不是自己偷偷跑第二套时间

---

## 10. 联机项目的最小稳态方案

如果你现在要做一个联机项目，最稳的简化方案是：

- UI 只负责点按和显示
- 地图负责玩法执行
- 系统负责全局流程
- UI 和地图主要靠广播衔接
- UI 尽量少直接改公共数据
- 公共状态只保留一份真值

这套方案虽然没有“全写在一个地方”那么省事，但后期会稳很多。

---

## 11. 和现有知识库的关系

建议与以下文档配合阅读：
- `docs/教程资料/专题研究/UI系统与切换机制解析.md`
- `docs/教程资料/专题研究/广播机制深度解析.md`
- `docs/脚本系统/专题研究/脚本作用域与数据流深度研究.md`
- `docs/脚本系统/专题研究/系统级脚本能力解析.md`
- `docs/引擎更新/专题研究/联机UI演进专题.md`
- `docs/脚本系统/专题研究/创游世界广播驱动项目结构实战.md`
- `docs/脚本系统/专题研究/创游世界格子UI与列表系统深度解析.md`
- `docs/脚本系统/专题研究/创游世界项目结构模板.md`
- `docs/元信息/创游世界术语表.md`

它们分别回答：
- UI 有哪些类型
- 广播为什么重要
- 数据该放哪一层
- 系统层管什么
- 联机 UI 为什么会出现新的推荐写法
- UI 和地图如何通过广播形成稳定链路
- 列表型 UI 应该如何组织刷新与数据源
- 项目整体结构该怎么拆
- 术语在全库里如何统一

---

## 12. 结论等级与待验证问题

### 已确认
- `[已确认]` 在联机环境下，UI 直接连续操作公共数据会引发同步等待，这是理解“上传并等待”问题的关键入口。
- `[已确认]` UI 更适合承担显示与输入职责，地图/系统层更适合承担公共状态与执行职责。

### 高可信推断
- `[高可信推断]` 采用“UI 发请求 → 地图/系统处理 → UI 刷新结果”的结构，能显著降低联机 UI 变重和后期难维护的风险。
- `[高可信推断]` 公共状态只保留一份真值，会比 UI 和地图各记一份更稳。

### 待验证
- `[待验证]` 不同版本下，某些组件和同步机制的细节边界仍可能变化，后续应继续结合更新说明与实测案例核对。

## 13. 证据与回查入口

- 主要 OCR 依据：`docs/OCR资料/引擎更新截图转文本.md`
- 编号映射入口：`docs/OCR资料/映射表/OCR 图片编号映射表.md`
- 已列出的来源截图编号可优先回查：`105506, 105508, 105510, 105513, 105515, 105516, 105535, 105537, 110352, 120605, 215554`
- 关联专题：`docs/引擎更新/专题研究/联机UI演进专题.md`、`docs/脚本系统/专题研究/脚本作用域与数据流深度研究.md`

## 14. 一句话结论

如果你想让创游世界的 UI 在联机里更稳：

> **让 UI 负责交互和显示，让地图/系统负责公共状态和实际执行，再用广播把两边接起来。**
