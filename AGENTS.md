# 创游世界资料库维护规则 (AGENTS.md)

## 项目信息

- **项目名称**：cysj-data（创游世界资料库）
- **项目类型**：创游世界资料库 + VitePress 文档网站
- **内容范围**：创游世界脚本、组件、编辑器界面、对象结构、引擎更新、教程截图、OCR 资料、项目设计、社区分析、维护报告等
- **工作区路径**：`/storage/emulated/0/创游世界资料汇总/`

## 允许修改范围

1. **文档目录**：`docs/` 及子目录（不含 .git、node_modules、dist）
2. **脚本目录**：`scripts/` 及子目录
3. **资料目录**：`资料/` 及子目录
4. **配置文件**：根目录下的 `.user.ini`、`.gitignore`、`.stignore`
5. **README.md**：根目录说明文件
6. **package.json**：依赖管理文件

## 禁止修改范围

1. **系统目录**：`.git/`、`.github/`
2. **构建产物**：`dist/`、`cache/`、`.temp/`、`site-dist/`、`.stfolder/`、`backup/`、`.backup/`
3. **依赖目录**：`node_modules/`
4. **锁定文件**：`pnpm-lock.yaml`、`LICENSE`
5. **部署脚本**：`auto-deploy.sh`、`deploy.sh`（如需修改请先说明风险）
6. **原始资料**：`资料/` 目录下的原始截图、原始文档不得删除

## 文档维护规则

### frontmatter 要求

每篇文档必须包含完整 frontmatter：

```yaml
---
title: 页面标题
description: 用一句话说明这篇文档解决什么问题
editor: 维护者昵称
status: 文档状态
difficulty: 难度级别
evidence: 资料来源
updated: YYYY-MM-DD
category: 所属分类
version: 适用版本
tags:
  - 创游世界
  - 相关标签
---
```

### 文档状态说明

| 状态 | 含义 |
|------|------|
| 草稿 | 初稿，内容不完整 |
| 整理中 | 正在整理结构 |
| 已结构化 | 结构完整，待填充内容 |
| 待验证 | 包含推测内容需确认 |
| 已复核 | 内容经过人工复核 |
| 持续维护 | 长期更新文档 |

### 证据等级说明

| 等级 | 含义 |
|------|------|
| E1 | 直接证据（官方文档、官方教程） |
| E2 | OCR/转写证据（截图转文字） |
| E3 | 归纳结论（从多个资料推断） |
| E4 | 社区经验/推测（待验证） |
| 资料整理 | 综合整理 |
| 待验证 | 需人工确认 |

### 内容规范

1. **保留原始资料**：OCR 原文必须保留，人工整理内容需分开
2. **标注不确定性**：推测内容必须标注"待验证"
3. **区分官方/推测**：不确定内容不能写成官方结论
4. **结构化写作**：重要结论先说，对新手友好
5. **SEO 友好**：包含关键词、内部链接、description
6. **VitePress 兼容**：使用 Markdown，不使用不兼容语法


### 内容标记处理

- 出现"待补充、待验证、待复核、TODO、未完成"需立即处理
- 完成的内容删除这些标记
- 未完成的内容标注为"草稿"状态

### 文件命名规范

1. 使用 kebab-case：`chuang-you-shi-jie-xin-shou-jiao-cheng.md`
2. 使用中文标题：`创游世界新手教程.md`
3. 避免特殊字符：不含空格、#、%等
4. 目录名使用 kebab-case：`ocr-zi-liao`

## 工作流程

### 文档修改流程

1. **检查阶段**：先读取文件，理解结构
2. **备份阶段**：不删除原始内容
3. **修改阶段**：使用 `apply_file` 修改文件
4. **测试阶段**：使用 `pnpm run docs:build` 测试构建
5. **提交阶段**：生成符合规范的 commit message

### 本轮维护限制

- 每轮最多新增 2 篇文档
- 每轮最多修改 5 篇文档
- 不要一次性创建大量空文档
- 不要为了数量制造低质量文档

### 测试命令

```bash
pnpm run docs:build    # 构建文档
pnpm run docs:preview  # 预览文档
pnpm run docs:ui:check # UI 检查
```


### Git 提交规范

| 类型 | 说明 |
|------|------|
| feat | 新功能、新文档 |
| fix | 修复问题 |
| style | UI/样式优化 |
| docs | 文档内容更新 |
| chore | 工程维护 |
| ci | 自动化流程 |
| refactor | 重构 |

## AI 检索优化

### 页面摘要

每页文档应包含清晰的一句话摘要，说明：
- 这篇文档解决什么问题
- 读者读完能学到什么
- 适合什么级别的读者

### 内部链接

1. 重要概念需链接到相关文档
2. 教程类文档需包含"下一步阅读"建议
3. 关联页面放在文档底部

### 标签体系

使用一致的标签：
- `创游世界`：主标签
- `脚本`、`组件`、`编辑器`、`变量`、`广播`、`UI`：功能标签
- `新手教程`、`进阶教程`、`高级教程`：难度标签
- `OCR资料`、`社区经验`：来源标签

## 自我复盘要求

每轮维护后必须进行自我评分：

- 内容准确性：20 分
- 新手可读性：20 分
- 结构清晰度：20 分
- AI 检索友好度：20 分
- 维护安全性：20 分

总分低于 85 分需继续修正，达到 85 分以上停止本轮。

---

*最后更新：2024-01-01*
*维护者：Azek431*
