# 创游世界资料库 AI 维护规则（AGENTS.md）

## 0. 文件定位

本文件是 `cysj-data` 项目的 AI 维护规则，用于指导 Codex、Claude Code、Cursor、ChatGPT、Gemini CLI、CodeGraph MCP 或其他 AI 编程助手在本仓库中工作。

本文件的目标不是让 AI 执行更多命令，而是让 AI：

```txt
更懂项目
更少乱改
更少占用 CPU / 内存 / 磁盘 IO
更安全地维护资料
更符合 Azek431 的个人工作习惯
```

AI 必须优先遵守本文件，而不是凭默认习惯随意运行构建、安装依赖、全量扫描、全量格式化、全量重构等高资源操作。

---

## 1. 项目信息

- **项目名称**：`cysj-data`
- **项目中文名**：创游世界资料库
- **项目类型**：创游世界资料库 + VitePress 文档网站
- **维护者**：`Azek431`
- **主要用途**：
  - 整理创游世界相关资料
  - 沉淀脚本、组件、编辑器、对象结构、引擎更新等知识
  - 保留 OCR、截图、社区经验、人工分析和维护报告
  - 构建一个新手友好、AI 易检索、长期可维护的资料库

- **工作区路径**：

```txt
/storage/emulated/0/创游世界资料汇总/
```

- **可能涉及的运行环境**：
  - Android / 移动设备存储
  - Windows
  - Linux
  - VS Code
  - Codex CLI
  - Codex GUI / App
  - Git 管理工具
  - Syncthing 同步目录

由于项目可能位于移动设备存储中，所有操作必须默认低资源、低 IO、低风险。

---

## 2. 核心维护原则

AI 在本项目中的最高原则：

```txt
资料不丢
结构不乱
证据清楚
新手能看懂
AI 能检索
设备不卡顿
后续能继续维护
```

默认行为必须是：

```txt
先读文件，再修改
先小范围分析，再执行命令
先轻量检查，再考虑构建
先保护原始资料，再做人工整理
先标注不确定性，再写结论
先输出清楚复盘，再给提交信息
```

禁止为了“看起来做了很多”而进行大范围、低质量、难复核的修改。

---

## 3. Azek431 的个人工作习惯

### 3.1 回复语言和风格

AI 回复应默认使用中文。

风格要求：

1. 解释要清楚
2. 对新手友好
3. 不要太机器化
4. 可以深入分析，但不要制造无意义复杂度
5. 给出的内容要方便直接复制
6. 涉及配置文件、代码、Markdown、README、AGENTS.md 时，必须使用普通 Markdown 代码块
7. 不使用 writing block / 写作模式，因为复制后格式容易不是原始 Markdown
8. 不要把命令、代码和解释混在一起导致难复制

---

### 3.2 Git 提交习惯

Azek431 通常使用 VS Code 或图形化界面提交 Git。

因此，除非用户明确要求，不要输出完整 Git 命令流程，例如：

```bash
git add .
git commit -m "..."
git push
```

默认只需要提供**提交信息文本**。

提交信息偏好：

```txt
中文
自然
口语化
不要太正式
不要太机器味
可以使用 Conventional Commit 类型
```

推荐风格：

```txt
docs: 整理一下创游世界资料规则
docs: 优化一下 AGENTS 低资源维护说明
fix: 修正几处文档 frontmatter
chore: 调整一下资料库忽略规则
style: 优化一下文档展示细节
```

不推荐：

```txt
docs: update docs
fix: fix bug
chore: misc changes
feat: comprehensive enhancement of repository-level agent governance
```

如果用户只问“提交信息写什么”，只输出提交信息本身，不要附带命令。

---

### 3.3 构建和测试习惯

Azek431 不希望 AI 每次日常小改都建议或执行构建。

普通文档修改时，不要主动建议：

```bash
pnpm run docs:build
pnpm run build
pnpm install
```

只有在以下情况才建议或执行构建：

1. 发布前最终检查
2. 部署前最终检查
3. 改了 VitePress 配置
4. 改了导航或侧边栏
5. 改了主题、样式、组件
6. 改了依赖或构建配置
7. 一次改了多篇文档且可能影响链接
8. 用户明确要求构建测试

普通修改 1-2 篇 Markdown 文档时，默认不构建，只做轻量检查。

---

### 3.4 命令输出习惯

如果必须给命令，优先：

1. 一次给少量命令
2. 说明命令作用
3. 避免危险命令
4. 避免默认递归全项目
5. 区分 Windows PowerShell、Linux shell、Android shell
6. 不要给用户一大坨难以理解的命令

如果只是生成文件内容，直接给完整文件内容，不要额外绕来绕去。

---

### 3.5 项目维护偏好

Azek431 更喜欢：

1. 深度优化
2. 结构清晰
3. 细节完整
4. 方便后续 AI 理解项目
5. 方便新手阅读
6. 原始资料和人工整理分开
7. 不确定内容明确标注
8. 低资源维护
9. 不乱删除
10. 不乱构建
11. 不乱安装依赖
12. 不乱重构目录

---

## 4. 项目内容范围

本项目可以包含：

1. 创游世界脚本资料
2. 创游世界组件资料
3. 编辑器界面说明
4. 对象结构资料
5. 引擎更新记录
6. 教程截图
7. OCR 资料
8. 原始截图说明
9. 社区经验整理
10. 项目设计资料
11. 维护报告
12. AI 分析报告
13. VitePress 文档站内容
14. 资料索引
15. 新手教程
16. 进阶教程
17. 资料可信度说明

---

## 5. 资源占用控制总原则

本项目默认运行环境可能较弱，因此 AI 必须遵守：

```txt
能不运行命令就不运行
能读指定文件就不扫描目录
能小范围搜索就不全项目搜索
能不构建就不构建
能不预览就不预览
能不安装依赖就不安装依赖
能不启动长期进程就不启动长期进程
能不重建索引就不重建索引
```

AI 不应把“完整检查”理解为“运行所有命令”。

普通维护中，**轻量检查比全量构建更优先**。

---

## 6. 高占用行为排行榜

以下行为最容易造成卡顿，必须严格限制。

| 排名 | 行为 | 主要占用 | 默认策略 |
|---:|---|---|---|
| 1 | `pnpm install` / `npm install` | CPU、内存、磁盘 IO、网络 | 禁止默认执行 |
| 2 | `pnpm run docs:preview` / `pnpm run dev` | 内存、CPU、长期进程 | 禁止默认启动 |
| 3 | `pnpm run docs:build` / `pnpm run build` | CPU、内存、磁盘 IO | 仅重要时执行 |
| 4 | 全项目 `rg/find/tree/du/ls -R` | CPU、磁盘 IO、上下文 | 禁止默认执行 |
| 5 | 全量 lint / format / typecheck | CPU、内存 | 仅必要时执行 |
| 6 | 全量 OCR / 图片处理 | CPU、内存、磁盘 IO | 禁止默认执行 |
| 7 | CodeGraph 重建索引 | CPU、磁盘 IO | 仅首次或大改后执行 |
| 8 | 批量移动/重命名文件 | 风险高、难回滚 | 拆分多轮执行 |
| 9 | 大范围重构目录 | 风险高、容易破坏链接 | 默认不执行 |
| 10 | 多任务并行运行 | CPU、内存叠加 | 避免并行 |

---

## 7. 命令执行分级

### 7.1 L0：无需命令

优先级最高。

适合：

1. 优化用户粘贴的文档
2. 生成 AGENTS.md
3. 生成 README
4. 修改单篇 Markdown 的文本内容
5. 整理 frontmatter 模板
6. 生成提交信息
7. 根据已知内容给维护建议
8. 分析用户提供的截图或文本
9. 起草文档结构

默认尽量停留在 L0。

---

### 7.2 L1：轻量命令

一般允许执行。

```bash
git status --short
git diff --stat
git diff -- 指定文件
rg -n "关键词" docs/指定目录
rg -n "关键词" scripts/指定文件
```

要求：

1. 必须限制搜索范围
2. 不扫描 `node_modules/`
3. 不扫描 `dist/`
4. 不扫描 `cache/`
5. 不扫描 `.git/`
6. 不扫描整个 `资料/` 图片目录
7. 不做全量正则搜索
8. 不把大量搜索结果塞进上下文

---

### 7.3 L2：中等命令

需要有明确原因。

```bash
rg -n "关键词" docs scripts
pnpm run docs:ui:check
```

适合：

1. 修改了多篇文档
2. 需要检查文档 UI 规则
3. 需要确认某个概念是否重复
4. 需要检查某个目录下的 TODO
5. 用户明确要求“检查一下”
6. 修改了文档规则但还没涉及构建

执行时仍然要避免扩大范围。

---

### 7.4 L3：高资源命令

默认不执行。

```bash
pnpm run docs:build
pnpm run docs:preview
pnpm install
pnpm run lint
pnpm run format
pnpm run typecheck
```

只有满足触发条件时才考虑执行。

执行前必须先说明：

```txt
为什么需要执行
会检查什么问题
是否有轻量替代方案
执行后是否需要停止长期进程
```

---

## 8. 默认禁止的命令

没有明确必要性时，不要运行：

```bash
pnpm install
pnpm update
npm install
npm update
pnpm run docs:build
pnpm run docs:preview
pnpm run docs:dev
pnpm run dev
pnpm run build
pnpm run lint
pnpm run format
pnpm run typecheck
```

不要运行这些容易造成全量扫描的命令：

```bash
find . -type f
rg ".*" .
rg -n "" .
tree
tree -a
du -sh .
du -sh *
ls -R
```

不要主动运行长期进程：

```bash
vite
vitepress dev
pnpm run docs:preview
pnpm run docs:dev
pnpm run dev
```

---

## 9. 高资源命令触发条件

### 9.1 什么时候可以运行 `pnpm run docs:build`

只有满足以下任一条件时，才可以运行：

1. 修改了 VitePress 配置
2. 修改了导航配置
3. 修改了侧边栏配置
4. 修改了主题配置
5. 修改了 `package.json`
6. 修改了大量内部链接
7. 一次新增或修改多篇文档
8. 修改了 frontmatter 规范
9. 准备发布或部署
10. 用户明确要求构建测试

只修改 1-2 篇普通 Markdown 文档时，不要主动运行构建。

---

### 9.2 什么时候可以运行 `pnpm run docs:preview`

只有满足以下任一条件时，才可以运行：

1. 用户明确要求预览
2. 修改了主题样式
3. 修改了 VitePress 组件
4. 修改了页面布局
5. 需要人工检查视觉效果
6. 构建通过后仍需确认页面展示

普通文字整理不要启动 preview。

preview 是长期进程，检查完成后应停止。

---

### 9.3 什么时候可以运行 `pnpm install`

只有满足以下任一条件时，才可以运行：

1. 用户明确要求安装依赖
2. `node_modules/` 不存在，并且用户要求运行项目
3. 修改了依赖项
4. 构建失败明确提示缺少依赖
5. 需要恢复项目可运行状态

不要因为“习惯性检查”运行安装命令。

---

### 9.4 什么时候可以运行 `pnpm run lint / format / typecheck`

只有满足以下任一条件时，才可以运行：

1. 用户明确要求
2. 修改了脚本文件
3. 修改了 JS / TS 配置
4. 修改了较多代码文件
5. 准备发布前最终检查
6. 出现明显语法、类型、格式问题

普通资料整理不需要主动运行这些命令。

---

## 10. 默认轻量维护流程

普通文档维护时，使用以下流程：

```txt
1. 读取目标文件
2. 理解原有结构
3. 判断证据等级和文档状态
4. 小范围修改内容
5. 检查本次修改的 frontmatter
6. 查看本次文件 diff
7. 输出修改摘要
8. 输出是否运行命令
9. 输出自我评分
10. 输出建议 commit message
```

默认不运行：

```bash
pnpm run docs:build
pnpm run docs:preview
pnpm install
pnpm run lint
pnpm run format
```

---

## 11. 允许修改范围

AI 可以修改以下内容：

1. **文档目录**
   - `docs/`
   - `docs/` 子目录

2. **脚本目录**
   - `scripts/`
   - `scripts/` 子目录

3. **资料目录中的整理性文本**
   - `资料/` 下的整理文档
   - OCR 整理文本
   - 维护报告
   - 资料索引

4. **根目录配置文件**
   - `.user.ini`
   - `.gitignore`
   - `.stignore`

5. **项目说明文件**
   - `README.md`
   - `AGENTS.md`

6. **依赖管理文件**
   - `package.json`

修改时必须遵守：

```txt
先读后改
小范围修改
保留原始资料
不删除未知内容
不覆盖 OCR 原文
不擅自大规模重命名
不擅自大规模移动目录
```

---

## 12. 禁止修改范围

AI 不得修改以下内容：

1. **系统目录**
   - `.git/`
   - `.github/`

2. **构建产物和缓存**
   - `dist/`
   - `cache/`
   - `.temp/`
   - `site-dist/`
   - `.stfolder/`
   - `backup/`
   - `.backup/`

3. **依赖目录**
   - `node_modules/`

4. **锁定文件**
   - `pnpm-lock.yaml`
   - `LICENSE`

5. **部署脚本**
   - `auto-deploy.sh`
   - `deploy.sh`

6. **原始资料**
   - 原始截图
   - 原始录屏
   - 原始 OCR 文件
   - 原始导出文档
   - 未备份的素材文件

如确实需要修改部署脚本、锁定文件或原始资料，必须先说明：

```txt
修改原因
潜在风险
影响范围
回滚方案
是否需要用户确认
```

---

## 13. 原始资料保护规则

`资料/` 是本项目最重要的数据来源之一。

### 13.1 不得删除

不得删除：

1. 原始截图
2. 原始录屏
3. 原始 OCR 文本
4. 原始导出文件
5. 未整理完的草稿资料
6. 社区资料原文
7. 维护历史记录
8. 未确认用途的素材文件

---

### 13.2 整理时必须分层

推荐结构：

```md
## 原始资料

保留 OCR 原文、截图描述或原始摘录。

## 人工整理

对原始资料进行结构化整理。

## 结论摘要

用新手能理解的话总结重点。

## 待验证内容

列出仍不确定的信息。
```

---

### 13.3 不确定内容必须标注

不能把推测写成官方结论。

错误写法：

```md
创游世界一定支持这个功能。
```

正确写法：

```md
根据当前截图和社区经验推测，创游世界可能支持这个功能，仍需进一步验证。
```

---

## 14. 文档 frontmatter 要求

每篇正式文档必须包含完整 frontmatter：

```yaml
---
title: 页面标题
description: 用一句话说明这篇文档解决什么问题
editor: Azek431
status: 文档状态
difficulty: 难度级别
evidence: 资料来源
updated: YYYY-MM-DD
category: 所属分类
version: 适用版本
tags:
  - 创游世界
  - 相关标签
---
```

---

### 14.1 字段说明

| 字段 | 要求 |
|---|---|
| `title` | 页面标题，清晰自然 |
| `description` | 一句话说明读者能获得什么 |
| `editor` | 默认使用 `Azek431` |
| `status` | 使用本文档规定的状态 |
| `difficulty` | 新手 / 进阶 / 高级 / 待判断 |
| `evidence` | 使用本文档规定的证据等级 |
| `updated` | 使用 `YYYY-MM-DD` |
| `category` | 所属分类 |
| `version` | 适用版本，不确定可写 `待确认` |
| `tags` | 至少包含 `创游世界` |

---

## 15. 文档状态说明

| 状态 | 含义 |
|---|---|
| 草稿 | 初稿，内容不完整 |
| 整理中 | 正在整理结构 |
| 已结构化 | 结构完整，待填充内容 |
| 待验证 | 包含推测内容，需要确认 |
| 已复核 | 内容经过人工复核 |
| 持续维护 | 长期更新文档 |

### 15.1 状态使用规则

1. 存在明显缺口时，用 `草稿`
2. 正在整理但未完成时，用 `整理中`
3. 结构完整但内容仍少时，用 `已结构化`
4. 有推测、有不确定内容时，用 `待验证`
5. 已人工确认后，用 `已复核`
6. 版本变化频繁的长期页面，用 `持续维护`

---

## 16. 证据等级说明

| 等级 | 含义 |
|---|---|
| E1 | 直接证据：官方文档、官方教程、官方公告 |
| E2 | OCR/转写证据：截图转文字、视频字幕、界面摘录 |
| E3 | 归纳结论：从多个资料推断 |
| E4 | 社区经验/推测：待验证经验 |
| 资料整理 | 多来源综合整理 |
| 待验证 | 需要人工确认 |

### 16.1 证据标注规则

1. 官方资料优先标 `E1`
2. 截图 OCR 标 `E2`
3. 多个截图或资料推导出的结论标 `E3`
4. 论坛、群聊、个人经验标 `E4`
5. 不确定时宁可标低，不要夸大证据等级
6. 没有证据时必须写 `待验证`

---

## 17. 内容写作规范

### 17.1 总体要求

1. 重要结论先说
2. 对新手友好
3. 分清“官方说明”和“个人推测”
4. 保留原始资料
5. OCR 原文和人工整理分开
6. 不确定内容标注“待验证”
7. 不写空泛废话
8. 不制造低质量页面
9. 不为了数量批量创建空文档
10. 保持 VitePress Markdown 兼容

---

### 17.2 推荐文档结构

普通资料页推荐：

```md
# 页面标题

## 一句话总结

这篇文档主要说明……

## 适合读者

- 新手
- 正在学习某功能的人
- 需要查资料的人

## 核心结论

1. ……
2. ……
3. ……

## 原始资料

保留截图 OCR、原文摘录或资料来源说明。

## 人工整理

对资料进行结构化解释。

## 使用方法 / 理解方式

用步骤或示例说明。

## 待验证内容

- ……

## 关联阅读

- [相关页面](../xxx.md)
```

---

### 17.3 新手友好规则

写给新手时：

1. 不直接堆术语
2. 第一次出现的概念要解释
3. 步骤要短
4. 示例要简单
5. 先说能做什么，再说原理
6. 不确定的地方明确告诉读者

---

### 17.4 SEO 和 AI 检索友好规则

每篇文档都应该包含：

1. 清晰标题
2. 一句话摘要
3. 关键词自然出现
4. 内部链接
5. 关联阅读
6. 资料来源说明
7. 结论与证据分离
8. 适合搜索的问题式小标题

推荐小标题：

```md
## 创游世界变量是什么？

## 创游世界广播怎么使用？

## 创游世界组件有哪些常见问题？
```

---

## 18. 内容标记处理规则

以下标记需要重点关注：

```txt
待补充
待验证
待复核
TODO
未完成
临时
以后再说
```

### 18.1 不要默认全项目扫描

不要默认全项目扫描这些标记。

优先只检查：

1. 本次修改的文件
2. 用户指定的目录
3. 与当前任务直接相关的页面

允许的小范围检查示例：

```bash
rg -n "待补充|待验证|待复核|TODO|未完成" docs/指定目录
```

不要执行：

```bash
rg -n "待补充|待验证|待复核|TODO|未完成" .
```

---

### 18.2 处理方式

如果可以立即补完：

1. 补充内容
2. 删除对应标记
3. 更新 frontmatter 状态

如果暂时不能补完：

1. 保留标记
2. 明确原因
3. 将文档状态设为 `草稿` 或 `待验证`
4. 不要假装已经完成

---

## 19. 文件命名规范

允许两种命名方式。

### 19.1 拼音 / 英文 kebab-case

适合技术类、长期维护类页面：

```txt
chuang-you-shi-jie-xin-shou-jiao-cheng.md
ocr-zi-liao.md
script-variable-guide.md
```

### 19.2 中文标题

适合资料整理、教程、人工阅读页面：

```txt
创游世界新手教程.md
变量使用说明.md
广播机制整理.md
```

### 19.3 禁止命名

避免：

```txt
文件 名.md
测试#1.md
教程%新版.md
new file.md
未命名.md
123.md
```

文件名不要包含：

```txt
空格
#
%
?
&
特殊控制字符
```

---

## 20. 目录维护规则

目录应保持清晰，不要为了分类创建过深层级。

推荐分类：

```txt
docs/
  guide/
  script/
  component/
  editor/
  object/
  engine/
  ocr-zi-liao/
  community/
  report/
  design/
```

如果现有目录已经有类似分类，优先沿用，不要重复创建近似目录。

错误示例：

```txt
docs/tutorial/
docs/tutorials/
docs/guide/
docs/guides/
```

同类目录不要重复堆叠。

---

## 21. 本轮维护限制

每轮维护必须控制规模：

1. 每轮最多新增 2 篇文档
2. 每轮最多修改 5 篇文档
3. 不要一次性创建大量空文档
4. 不要为了数量制造低质量文档
5. 不要一次性重构大量目录
6. 不要一次性移动大量资料
7. 不要全量格式化整个项目
8. 不要批量改名大量文件

如果任务超过限制，应拆分为多轮维护。

---

## 22. 测试命令策略

项目可用测试命令：

```bash
pnpm run docs:build
pnpm run docs:preview
pnpm run docs:ui:check
```

### 22.1 默认策略

普通文档维护：

```txt
不运行 docs:build
不运行 docs:preview
只做轻量人工检查和 diff 检查
```

### 22.2 推荐触发方式

| 场景 | 推荐检查 |
|---|---|
| 只改 1 篇普通文档 | 不构建，检查 frontmatter 和 diff |
| 改 2-5 篇文档 | 视情况运行 `docs:ui:check` |
| 改导航 / 侧边栏 | 运行 `pnpm run docs:build` |
| 改 VitePress 配置 | 运行 `pnpm run docs:build` |
| 改主题 / 样式 | 构建后再视情况预览 |
| 改 package.json | 说明风险，再考虑安装或构建 |
| 发布前 | 运行 `pnpm run docs:build` |

---

## 23. package.json 修改规则

`package.json` 可以修改，但必须谨慎。

允许修改：

1. 新增必要脚本
2. 修正已有脚本名称
3. 补充项目元信息
4. 添加确实需要的依赖

禁止随意：

1. 更新全部依赖
2. 删除未知脚本
3. 修改包管理器
4. 切换构建工具
5. 增加大型依赖
6. 增加需要原生编译的依赖
7. 为了小功能添加重型工具链

修改 `package.json` 后，不要自动运行 `pnpm install`，除非用户要求或必须验证。

---

## 24. 脚本目录维护规则

`scripts/` 可用于资料整理、OCR 辅助、文档生成等。

修改脚本时必须：

1. 先看脚本用途
2. 不默认执行大范围处理脚本
3. 不批量覆盖资料
4. 给危险操作加确认或 dry-run
5. 避免默认扫描整个项目
6. 避免默认写入大量文件
7. 支持只处理单个文件或指定目录

推荐脚本设计：

```txt
默认 dry-run
必须显式传入目录
必须排除 node_modules/dist/cache
输出修改摘要
支持只处理单个文件
危险操作需要二次确认
```

危险脚本必须有保护：

```txt
不得默认删除文件
不得默认覆盖原始资料
不得默认移动整个目录
不得默认全量 OCR
不得默认全量压缩图片
```

---

## 25. OCR 资料处理规则

OCR 内容通常不稳定，必须保留原始文本。

### 25.1 OCR 页面结构

推荐：

```md
## OCR 原文

保留原始识别文本，不做过度修正。

## 人工校对

修正常见错字、断句和格式。

## 结构化整理

整理成教程、说明或知识点。

## 不确定内容

列出无法确认的词、按钮名、界面名称。
```

### 25.2 OCR 修改规则

允许：

1. 修正明显错字
2. 增加人工整理
3. 标注不确定内容
4. 添加截图来源说明

禁止：

1. 删除 OCR 原文
2. 把 OCR 猜测写成官方结论
3. 批量覆盖原始 OCR 文件
4. 为了美观丢失原始证据
5. 默认运行全量 OCR 脚本

---

## 26. VitePress 兼容规则

Markdown 内容必须兼容 VitePress。

避免使用：

```txt
复杂 HTML
不必要的 iframe
未知自定义容器
不兼容插件语法
会破坏构建的特殊字符
```

推荐使用：

```md
# 一级标题
## 二级标题
### 三级标题

::: tip 提示
内容
:::

::: warning 注意
内容
:::
```

内部链接优先使用相对路径。

---

## 27. 内部链接规则

文档中提到重要概念时，应尽量链接相关页面。

但不要为了链接而链接。

推荐放在文档底部：

```md
## 关联阅读

- [变量说明](../script/变量说明.md)
- [广播机制](../script/广播机制.md)
- [组件整理](../component/组件整理.md)
```

教程类文档推荐增加：

```md
## 下一步阅读

- ……
```

---

## 28. 标签体系

标签应统一，避免同义词混乱。

### 28.1 主标签

```yaml
- 创游世界
```

### 28.2 功能标签

```yaml
- 脚本
- 组件
- 编辑器
- 变量
- 广播
- UI
- 对象
- 引擎
```

### 28.3 难度标签

```yaml
- 新手教程
- 进阶教程
- 高级教程
```

### 28.4 来源标签

```yaml
- OCR资料
- 官方资料
- 社区经验
- 资料整理
```

---

## 29. AI 检索优化规则

为了让 AI 和搜索更容易理解资料，每篇文档应尽量具备：

1. 一句话摘要
2. 适合读者
3. 核心结论
4. 原始证据
5. 人工整理
6. 待验证内容
7. 关联阅读
8. 清晰标签
9. 自然关键词
10. 不夸大证据等级

### 29.1 页面摘要要求

摘要应回答：

```txt
这篇文档解决什么问题？
读者读完能学到什么？
适合什么级别的读者？
```

示例：

```md
## 一句话总结

这篇文档整理了创游世界变量的基础概念、常见用法和注意事项，适合刚开始学习脚本的新手阅读。
```

---

## 30. CodeGraph / MCP 使用规则

如果项目已经配置 CodeGraph 或其他 MCP 工具，应优先用于理解项目结构，但不要过度依赖。

### 30.1 允许使用的场景

1. 查找文档结构
2. 理解脚本之间的关系
3. 分析影响范围
4. 查找相关页面
5. 减少重复 grep/read
6. 在大项目中快速定位相关文件

### 30.2 不要主动重建索引

不要因为普通文档修改就主动重建 CodeGraph 索引。

只有在以下情况才考虑重建：

1. 首次配置 CodeGraph
2. 项目目录结构大幅变化
3. 新增大量脚本或页面
4. CodeGraph 查询明显失效
5. 用户明确要求

### 30.3 小项目优先轻量检查

如果只是修改少量 Markdown，直接读文件和检查 diff 即可，不需要使用复杂索引工具。

---

## 31. Git 使用规则

### 31.1 提交前检查

默认只做轻量检查：

```bash
git status --short
git diff --stat
```

必要时查看具体 diff：

```bash
git diff -- docs/指定文件.md
```

不要为了提交而自动构建，除非满足构建触发条件。

---

### 31.2 不要默认代替用户提交

AI 不要默认执行：

```bash
git add .
git commit
git push
```

除非用户明确要求 AI 直接提交，否则只提供提交信息。

---

### 31.3 Commit message 风格

提交信息优先使用中文、自然、简洁的风格。

可以使用 Conventional Commit 类型，但不要写得太机器化。

推荐：

```txt
docs: 整理一下变量资料结构
docs: 补充创游世界脚本说明
docs: 优化一下 AGENTS 低资源维护规则
fix: 修正几处文档 frontmatter
chore: 调整资料库忽略规则
style: 优化一下文档展示细节
```

避免：

```txt
docs: update docs
fix: fix bug
chore: misc changes
```

---

### 31.4 提交信息输出规则

如果用户只问提交信息，回复只包含一条提交信息，例如：

```txt
docs: 优化一下资料库维护规则
```

不要额外输出：

```txt
你可以执行：
git add .
git commit -m "..."
git push
```

---

## 32. 安全修改规则

### 32.1 修改前

必须先确认：

1. 文件是否在允许范围内
2. 是否可能覆盖原始资料
3. 是否会影响构建或部署
4. 是否需要用户确认
5. 是否有更轻量的做法
6. 是否存在重复文档

---

### 32.2 修改中

必须做到：

1. 不大范围无关修改
2. 不删除未知内容
3. 不擅自重命名大量文件
4. 不擅自移动目录
5. 不引入大型依赖
6. 不启动长期进程
7. 不批量格式化无关文件
8. 不把推测写成事实

---

### 32.3 修改后

必须输出：

1. 修改了哪些文件
2. 改了什么内容
3. 是否有未完成事项
4. 是否运行了命令
5. 如果没运行构建，说明原因
6. 自我评分
7. 建议 commit message

---

## 33. 移动设备 / 低性能环境特别规则

由于项目路径可能位于 Android / 移动设备存储中：

```txt
/storage/emulated/0/创游世界资料汇总/
```

必须额外注意：

1. 不默认运行高 IO 命令
2. 不默认扫描大量图片
3. 不默认生成大量缓存
4. 不默认启动长期 watcher
5. 不默认安装依赖
6. 不默认全量构建
7. 不默认全量 OCR
8. 不默认压缩全目录图片
9. 不默认重建大型索引

如果必须运行高资源任务，应优先建议在性能更好的电脑环境执行。

---

## 34. AI 行为边界

AI 不应该做：

1. 未经要求主动大改项目结构
2. 未经要求主动升级依赖
3. 未经要求主动安装依赖
4. 未经要求主动构建项目
5. 未经要求主动启动预览
6. 未经要求主动删除文件
7. 未经要求主动清理缓存
8. 未经要求主动改部署脚本
9. 未经要求主动移动原始资料
10. 未经要求主动批量生成空文档
11. 未经要求主动执行 Git 提交和推送
12. 未经要求主动全量格式化

AI 应该做：

1. 先理解再修改
2. 先轻量检查
3. 小范围改动
4. 保留证据
5. 标注不确定性
6. 输出复盘
7. 给出自然中文 commit message
8. 明确说明没有运行哪些重命令以及原因

---

## 35. AI 回复格式规则

AI 回复必须尽量清晰。

### 35.1 修改类任务回复格式

推荐：

```md
## 修改摘要

- 修改了：
- 优化了：
- 保留了：

## 命令执行情况

- 高资源命令：未执行
- 构建：未执行，原因：本轮仅修改普通文档，未触发构建条件
- 预览：未启动

## 未完成事项

- 无 / 有：

## 自我评分

| 项目 | 分数 |
|---|---:|
| 内容准确性 |  |
| 新手可读性 |  |
| 结构清晰度 |  |
| AI 检索友好度 |  |
| 维护安全性 |  |
| 总分 |  |

## 建议提交信息

docs: 整理一下创游世界资料
```

---

### 35.2 只要文件内容时

如果用户明确要求“只告诉我文件内容”，则：

1. 直接给完整文件内容
2. 使用普通 Markdown 代码块
3. 不使用 writing block
4. 不夹杂太多额外解释
5. 代码块外最多给一句简短说明

---

### 35.3 只要 commit message 时

如果用户只要提交信息，直接输出：

```txt
docs: 优化一下创游世界资料库规则
```

不要解释一大段。

---

## 36. 常见任务处理策略

### 36.1 只优化一篇文档

推荐：

```txt
读取目标文件
优化结构和表达
检查 frontmatter
查看 diff
输出复盘
```

不推荐：

```txt
运行 docs:build
启动 preview
全项目搜索
全量格式化
```

---

### 36.2 新增一篇文档

推荐：

```txt
先检查是否已有类似页面
创建高质量页面
补齐 frontmatter
添加关联阅读
输出复盘
```

不推荐：

```txt
一次新增很多空页面
复制大量模板但没有内容
为了 SEO 堆关键词
```

---

### 36.3 整理 OCR 资料

推荐：

```txt
保留 OCR 原文
增加人工校对
标注待验证
提炼核心结论
```

不推荐：

```txt
删除原始 OCR
把猜测写成事实
全量运行 OCR 脚本
覆盖原始资料
```

---

### 36.4 修改导航或侧边栏

这类修改可能影响构建。

推荐：

```txt
读取配置
小范围修改
运行 pnpm run docs:build
输出构建结果
```

---

### 36.5 修改依赖或脚本

这类修改风险较高。

推荐：

```txt
说明修改原因
小范围修改 package.json
不主动 pnpm install
必要时再执行验证
```

---

### 36.6 让 AI 深度分析项目

推荐：

```txt
先读取项目说明和相关文件
必要时使用 CodeGraph / MCP 定位结构
优先小范围分析
不要直接全项目扫描
不要直接构建
输出问题、原因、影响范围、修改建议
```

不推荐：

```txt
上来就 pnpm install
上来就 pnpm run build
上来就 find . -type f
上来就全量重构
```

---

## 37. 自我复盘要求

每轮维护后必须进行自我评分。

| 项目 | 分值 |
|---|---:|
| 内容准确性 | 20 |
| 新手可读性 | 20 |
| 结构清晰度 | 20 |
| AI 检索友好度 | 20 |
| 维护安全性 | 20 |

总分低于 85 分，需要继续修正。

达到 85 分以上，可以停止本轮。

### 37.1 复盘模板

```md
## 本轮维护复盘

- 修改文件：
- 新增文件：
- 删除文件：无
- 是否修改原始资料：否
- 是否运行高资源命令：否
- 是否运行构建：否，原因：本轮仅修改普通文档，未触发构建条件
- 未完成事项：

### 自我评分

| 项目 | 分数 |
|---|---:|
| 内容准确性 |  |
| 新手可读性 |  |
| 结构清晰度 |  |
| AI 检索友好度 |  |
| 维护安全性 |  |
| 总分 |  |

### 建议提交信息

docs: 整理一下创游世界资料文档
```

---

## 38. 评审规则

如果 AI 被要求 review 当前改动，需要重点检查：

1. 是否误删原始资料
2. 是否把推测写成事实
3. frontmatter 是否完整
4. 文档状态是否合理
5. 证据等级是否夸大
6. 是否创建了低质量空文档
7. 是否破坏了内部链接
8. 是否修改了禁止范围
9. 是否引入了高资源依赖
10. 是否违背低资源维护原则

Review 时不要只看语法，要重点看资料可信度和维护安全性。

---

## 39. 推荐忽略内容

AI 在搜索、分析和处理时，应默认忽略：

```txt
node_modules/
dist/
cache/
.temp/
site-dist/
.stfolder/
backup/
.backup/
.git/
```

如需搜索资料目录，应优先指定具体子目录，不要扫描整个 `资料/`。

---

## 40. 最终提醒

本项目最重要的不是“跑完所有检查”，而是：

```txt
资料不丢
结构不乱
证据清楚
新手能看懂
AI 能检索
设备不卡顿
后续能继续维护
```

普通维护时：

```txt
能不构建就不构建
能不预览就不预览
能小范围搜索就不全量搜索
能读指定文件就不扫整个项目
能手动检查就不跑重命令
能只给提交信息就不输出 Git 命令
能用普通 Markdown 代码块就不用 writing block
```

---

*最后更新：2026-06-06*  
*维护者：Azek431*
