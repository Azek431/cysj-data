实际上创游编辑器是使用js来编写的，甚至模块化转js代码的时候是你点击保存的时候，而不是在你点击试玩或发布阶段临时编译的，而且创游之所以有一堆bug，其实是因为套了一堆try然后又不对传入的对象做处理，就等着玩家catch出错误来修复。骗你的，实际上因为没处理传入参数（只有特定的一些），实际上某些bug连报错都没有。你很难想象创游连给地图单独写一个对象都不肯，地图物体本身和当前地图居然是共用一个对象的

一个地图的结构: 当前地图
┗背景
┗自定义层级
    ┗图层1
        ┗未命名
            ┗（*用来管理地块的）
        ┗（*这是物体）
        ┗（*这是物体）
         ……
    ┗（*其他层级）
┗镜头

用这种办法可以做出任何类型的数组（有一些如配置表，或含指令或属性的积木不可加入）
对了，不知道你知不知道这个
就是在3.x中，可以用“查看数值”来查看一个“脚本”属性中的代码
就是因为这个，我试图在创游里写代码，然后想要通过某种方式把代码（文本类型）转换成脚本类型，但是我一直找不要办法，而且经我观察，代码内部应该是有个生成器，来根据积木块所在位置等一些因素来生成一串文本（可能是哈希吧，我也不确定）
就是因为这个，我试图在创游里写代码，然后想要通过某种方式把代码（文本类型）转换成脚本类型，但是我一直找不要办法，而且经我观察，代码内部应该是有个生成器，来根据积木块所在位置等一些因素来生成一串文本（可能是哈希吧，我也不确定）
对了，这个我搞不懂，为啥有的时候可以进行类型转换，有的时候却转换失败。
就比如说我在一个数字数组里放置真假值，在显示这个数组里的真假值的值，true是1，false是0
当然，创游内部不是也有一个类型转换积木吗，就是文本拼接，参数几何可以填上任何类型，然后转换为文本类型

只看变量（就是local.v1这些）
因为文本变量是用“""”括起来的，原本应该可以给文本改为其他的文本就能实现写代码，但是创游不笨啊，它用\来转义“"”和“\”，这种办法估计是无解的，所以这种通过修改文本来修改代码的办法就不能使用了
对了，你也可以通过“文本拼接”来把代码转换为文本，并复制到剪贴板
对了，这个只能在3.x中查看（脚本代码）
你也是可以把创游所有的积木都看一遍（因为我没那么多时间）
我之前一直在找如何把文本类型转换为脚本类型，但一直没成功
喔，不可以通过“拼接文本”来获取
但我有办法😊
这个是这个视频里的文本
(evtParam) => {
return this.getRuntime()?.awaiter(this, function* () {
  try{
    let bp=false,
      fId="11358168_CustomComp_BrFfizH158owN_kokZfyV7N",
      eId=this.gameObject.eId++,
      local={evtParam};
    

if(bp){
  bp = yield this.getRuntime().debug.awaitClickExecute(this.gameObject,eId,6);
}
this.getRuntime().debug.addLog(this.gameObject, "BrH4hbcfwW2Qh","数字",123);


if(bp){
  bp = yield this.getRuntime().debug.awaitClickExecute(this.gameObject,eId,7);
}
this.getRuntime().debug.addLog(this.gameObject, "BrH4hbfUC26sh","真假值",true);


if(bp){
  bp = yield this.getRuntime().debug.awaitClickExecute(this.gameObject,eId,8);
}
this.getRuntime().debug.addLog(this.gameObject, "BrH4hbjzH8a4h","文字","ABC");



        if(bp){
      yield this.getRuntime().debug.awaitClickEnd(this.gameObject,eId);
    }
  }catch(e){
    cc.log(e)
  }
});}
你也能自己试试，这样的话，你自己可以尽量把（3.x）所有的积木所转换的代码都看到
因为3.x不支持4.x的部分积木
对于我而言，感觉获取（伪）源代码应该会更有效一些，但是最大的缺点就是技术问题；我刚才演示的也不是不行，技术成本大大降低，但最大的缺点应该是获取的代码量会比较少
而且获取的代码中还有一部分是类似哈希的随机文本（0~9和小写与大写的26个字母）生成


